import React from "react"
import styled from "styled-components"

//Set up grid for page below. Preferrably Mobile-First.
const PageWrapper = styled.div`
  display: grid;
  grid-template-areas:
    "hero"
    "about"
    "cutomers"
    "brands"
    "deals"
    "coupons"
    "storeInfo"
    "map";
  
`

export default () => (


)